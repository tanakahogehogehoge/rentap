<div class="wrapper  col-md-offset-3 col-md-6 col-md-offset-3 col-sm-offset-1 col-sm-10 col-sm-offset-1">
  <table class="table table-hover">
    <thead>
      <h3>メッセージ一覧</h3>
    </thead>
    <tbody>
      <% @conversations.each do |conversation| %>
        <td>
          <% if conversation.target_user(current_user).present? %>
            <%= link_to conversation.target_user(current_user).name, conversation_messages_path(conversation)%>
          <% end %>          
        </td>
      <% end %>
    </tbody>
  </table>
</div>